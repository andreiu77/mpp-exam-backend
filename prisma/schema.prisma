generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Voter {
  id        String     @id
  voted     Int?    // candidate id, nullable if not voted
}